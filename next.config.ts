import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configurations de production de Next.js.
  // Pour l'instant, une configuration minimale est suffisante.
};

export default nextConfig;