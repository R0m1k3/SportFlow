# Backend server configuration
BACKEND_PORT=3001
FRONTEND_URL=http://localhost:8080

# Database connection
# For local development outside Docker, you might change DB_HOST to 'localhost'.
# Inside Docker, the host will be 'db' (the service name from docker-compose.yml).
DB_HOST=localhost
DB_PORT=5435
DB_USER=sporflow_user
DB_PASSWORD=change_this_password
DB_NAME=sporflow_db