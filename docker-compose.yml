version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nextjs-activity-tracker
    ports:
      - "3500:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - nginx_default

networks:
  nginx_default:
    external: true