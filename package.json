{
  "name": "sporflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:backend": "cd backend && npm run build",
    "start:backend": "cd backend && npm start"
  },
  "dependencies": {
    // Keep all existing frontend dependencies
  },
  "devDependencies": {
    // Keep all existing dev dependencies
  }
}